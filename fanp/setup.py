from setuptools import setup, find_packages

setup(
    name="fanp",
    version="0.1.0",
    description="Forgetting-Aware Neural Network Pruning",
    packages=find_packages(),
    python_requires=">=3.12",
)
